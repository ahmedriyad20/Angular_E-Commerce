export interface Iproduct {
    id: number,
    title: string,
    price: number,
    description: string,
    category: string,
    rating: number,
    image: string,
    images: string[]
    thumbnail: string
    
}

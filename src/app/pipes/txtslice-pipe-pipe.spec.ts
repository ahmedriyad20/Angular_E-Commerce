import { TxtslicePipePipe } from './txtslice-pipe-pipe';

describe('TxtslicePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TxtslicePipePipe();
    expect(pipe).toBeTruthy();
  });
});
